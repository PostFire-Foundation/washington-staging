# washington-staging

**Staging mirror. NOT the live site.** noindex, forms dead, GA stripped. Built from washington-site by `staging_build.py`. The A2 folder prototype is at `/a2/` until integration lands.
